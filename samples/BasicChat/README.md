# BasicChat

Demonstrates a single-turn, non-streaming chat request using `IOllamaHttpClient.SendChat`.

**Model required:** `llama3.2` (or any chat model — edit `Program.cs` to change it).

```bash
ollama pull llama3.2
dotnet run --project samples/BasicChat/BasicChat.csproj
```
